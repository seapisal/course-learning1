import 'package:flutter/material.dart';

class CourseMarketing extends StatelessWidget {
  const CourseMarketing({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Course Marketing'),
      ),
    );
  }
}
