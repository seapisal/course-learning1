import 'package:flutter/material.dart';

class FavorithPage extends StatelessWidget {
  const FavorithPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Favorith Page',style: TextStyle(fontSize: 40),)),
    );
  }
}
