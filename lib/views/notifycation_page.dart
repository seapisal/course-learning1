import 'package:flutter/material.dart';

class NotifitcationPage extends StatelessWidget {
  const NotifitcationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Notification Page',style: TextStyle(fontSize: 40),)),
    );
  }
}
